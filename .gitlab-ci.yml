stages:
  - build
  - test

build:
  stage: build
  script:
    - sudo apt install libfreetype6-dev
    - cargo build
test:
  stage: test
  script:
    - cargo +nightly fmt -- --check
    - cargo test
  dependencies:
    - build
  variables:
    GIT_STRATEGY: none
