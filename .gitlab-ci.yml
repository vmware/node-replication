stages:
  - build
  - test

build:
  stage: build
  script:
    - sudo apt install libfreetype6-dev gnuplot --yes
    - cargo build
test:
  stage: test
  script:
    - cargo +nightly fmt -- --check
    - cargo test
    - cargo bench
  dependencies:
    - build
  variables:
    GIT_STRATEGY: none
